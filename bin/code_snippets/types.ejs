'use strict';
module.exports = function({
    GraphQLObjectType,
    GraphQLSchema,
    GraphQLString,
    GraphQLList,
    GraphQLNonNull
}) {
    const <%= type_name %> = new GraphQLObjectType({
    name: '<%= `${type_name}` %>' ,
        description: '...',

        fields: () => ({<% for (let [field, type] of Object.entries(field_tables)) { %>
            <%= field %>: {
                type: <%= type %>,
            },
        <% } %>})
    });
    return <%= type_name %>;
}
